# =============================================================================
# docker-compose.yml  (project root)
# Services: backend (FastAPI) · redis · frontend (Node/Vite)
#
# ┌─────────────┐     ┌─────────────┐
# │   Browser   │────▶│   backend   │
# └─────────────┘     └──────┬──────┘
#                            │
#                                          ┌──────▼──────┐
#                                          │    redis    │
#                                          └─────────────┘
#
# Volumes:
#   model-data  → GGUF + Piper .onnx model files (read-only)
#   db-data     → SQLite sessions.db persistence
#   redis-data  → Redis RDB snapshot persistence
# =============================================================================

version: "3.9"

services:

  # ---------------------------------------------------------------------------
  # REDIS — session store for horizontal scaling
  # ---------------------------------------------------------------------------
  redis:
    image: redis:7.2-alpine
    restart: unless-stopped
    command: >
      redis-server
      --save 60 1
      --loglevel warning
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # BACKEND — FastAPI + llama-cpp-python + local STT/TTS
  # ---------------------------------------------------------------------------
  backend:
    user: "root"
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_URL=redis://redis:6379/0
      - MODEL_PATH=/models/qwen2.5-3b-instruct-q4_k_m.gguf
      # Piper TTS voice model path (matching .onnx.json must exist in same dir)
      - PIPER_MODEL=/models/en_US-lessac-medium.onnx
      - FRONTEND_ORIGIN=${FRONTEND_ORIGIN:-http://localhost:3000}
      - JWT_SECRET=${JWT_SECRET:-CHANGE_ME_IN_PRODUCTION}
      - PYTHONUNBUFFERED=1
    volumes:
      - db-data:/data
      - ./models:/models:ro
      - ./chroma_db:/app/chroma_db
      - ./dataset:/app/data:ro
      - ./backend/src:/app/src       # Live code reload
      - hf-cache:/root/.cache/huggingface # Model cache

    networks:
      - internal
      - rag-net

    ports:
      - "8000:8000"

  # ---------------------------------------------------------------------------
  # FRONTEND DEV — Vite dev server with HMR
  #   docker compose up frontend-dev
  #   → React app at http://localhost:3000 (proxies /api + /ws to backend:8000)
  # ---------------------------------------------------------------------------
  frontend-dev:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    working_dir: /app
    volumes:
      - ./frontend/src:/app/src          # live source for HMR
      - ./frontend/index.html:/app/index.html
      - ./frontend/vite.config.js:/app/vite.config.js
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    depends_on:
      - backend
    networks:
      - internal
      - external

  # ---------------------------------------------------------------------------
  # FRONTEND PROD — multi-stage build → node serve /dist
  #   docker compose up frontend
  #   → Optimised static bundle at http://localhost:8080
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - internal
      - external


networks:
  internal:
    driver: bridge
  rag-net:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: "1400"
  external:
    driver: bridge


volumes:
  redis-data:
  db-data:
  hf-cache: