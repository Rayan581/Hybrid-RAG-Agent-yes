FROM node:20-alpine

WORKDIR /app

# Install deps at build time — startup is instant
COPY package*.json ./
RUN npm install

# Source is mounted at runtime via volume (HMR works)
EXPOSE 3000

CMD ["npm", "run", "dev"]
